# Basic screen configuration to fix some defaults

nonblock 5

# Detach the screen if disconnected
autodetach on

# Show messages at the bottom for only 1s
msgwait 1

# Allow for 10,000 lines of scroll back terrible default of 100
defscrollback 10000
